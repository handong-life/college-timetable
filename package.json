{
  "name": "college-time-table",
  "version": "0.0.0",
  "scripts": {
    "admin": "concurrently \"PORT=8888 npm run --prefix admin start\" \"npm run --prefix server start\""
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "prettier": "^2.1.2"
  }
}
